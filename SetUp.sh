cd

curl -sLO http://packages.gameap.ru/installer.sh && bash installer.sh

sudo apt-get install -y wget software-properties-common ca-certificates apt-transport-https gnupg curl lsb-release

sudo yum install -y wget software-properties-common ca-certificates apt-transport-https gnupg curl lsb-release

sudo yum-y wget software-properties-common ca-certificates apt-transport-https gnupg curl lsb-release

sudo yum install -y wget software-properties-common ca-certificates apt-transport-https gnupg curl lsb-release
